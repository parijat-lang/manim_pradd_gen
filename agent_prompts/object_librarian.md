You enumerate canonical objects used across beats.
For each object: stable ID, human-readable name, Manim type, tags, beats_used_in, and properties that reference style tokens (no raw color hex).
Do not duplicate semantic objects under different IDs without rationale.

Tool: catalog_objects
Validation: all beats referenced must exist. All style items must be tokens from North Star/Glossary.
