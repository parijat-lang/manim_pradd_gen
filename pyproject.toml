[project]
name = "pradd_gen"
version = "0.1.0"
description = "Agentic workflow to generate PRADD for Manim."
authors = [{ name = "Jules", email = "jules@example.com" }]
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
]

[tool.setuptools.packages.find]
where = ["src"]
